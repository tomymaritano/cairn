---
"cairn-ui": minor
---

Two new tour primitives: `<CairnProgress>` (step "N / total" + dot indicators)
and `<CairnBeacon>` (a pulsing, accessible click-to-continue hotspot anchored to
the current step's target; respects `prefers-reduced-motion`).
